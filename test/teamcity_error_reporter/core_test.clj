(ns teamcity-error-reporter.core-test
  (:require [clojure.test :refer :all]
            [teamcity-error-reporter.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
